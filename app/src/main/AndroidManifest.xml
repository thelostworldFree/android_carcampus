<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ljheee.CarCampus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/bus"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".mainone.LocationActivity" />
        <activity android:name=".mainone.BookingActivity" />
        <activity android:name=".mainone.PayActivity" />
        <activity android:name=".Information" />
        <activity android:name=".Setup" />
        <activity android:name=".mainone.OrderActivity" />
        <activity android:name=".mainone.CollectionActivity" />
        <activity android:name=".mainone.GoutActivity"></activity>
        <activity android:name=".mainone.AboutActivity" />
        <activity android:name=".mainone.RemindActivity"></activity>

    </application>

</manifest>